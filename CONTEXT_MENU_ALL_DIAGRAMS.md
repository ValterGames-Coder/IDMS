# Контекстное меню для всех типов диаграмм

## ✅ Что добавлено

Контекстное меню по **правой кнопке мыши** теперь работает для всех типов диаграмм: **BPMN**, **ERD** и **DFD**.

## 🎯 Функционал по типам диаграмм

### 📊 ERD (Entity Relationship Diagram)
При клике ПКМ на Entity или Relationship:
```
┌─────────────────────────┐
│ ✏️  Edit Attributes     │
│ 📝  Rename              │
│ ─────────────────────── │
│ 🗑️  Delete             │
└─────────────────────────┘
```

**Доступные действия:**
1. ✏️ **Edit Attributes** - открывает модальное окно для редактирования атрибутов
2. 📝 **Rename** - быстрое переименование элемента
3. 🗑️ **Delete** - удаление элемента со всеми связями

### 🔄 BPMN (Business Process Model and Notation)
При клике ПКМ на любой элемент (задачи, события, шлюзы и т.д.):
```
┌─────────────────────────┐
│ 📝  Rename              │
│ ─────────────────────── │
│ 🗑️  Delete             │
└─────────────────────────┘
```

**Доступные действия:**
1. 📝 **Rename** - переименование элемента
2. 🗑️ **Delete** - удаление элемента со всеми связями

### 📈 DFD (Data Flow Diagram)
При клике ПКМ на любой элемент (процессы, хранилища, внешние сущности):
```
┌─────────────────────────┐
│ 📝  Rename              │
│ ─────────────────────── │
│ 🗑️  Delete             │
└─────────────────────────┘
```

**Доступные действия:**
1. 📝 **Rename** - переименование элемента
2. 🗑️ **Delete** - удаление элемента со всеми связями

## 🎮 Как использовать

### Шаг 1: Открыть контекстное меню
- **ПКМ** (правая кнопка мыши) на любом элементе диаграммы

### Шаг 2: Выбрать действие
- Кликните на нужное действие в меню

### Особенности:
- ⚡ Меню закрывается автоматически при клике вне его
- 🔒 Меню недоступно, если диаграмма заблокирована другим пользователем
- ✅ Успешное удаление показывает уведомление с именем удаленного элемента
- 🔗 При удалении элемента автоматически удаляются все его связи

## 💡 Примеры использования

### BPMN - Удаление задачи
```
1. ПКМ на задаче "Approve Request"
2. Выбрать "Delete"
3. ✅ Уведомление: "Approve Request deleted"
```

### ERD - Редактирование Entity
```
1. ПКМ на Entity "User"
2. Выбрать "Edit Attributes"
3. Добавить атрибуты в модальном окне
4. Сохранить изменения
```

### DFD - Переименование процесса
```
1. ПКМ на процессе "Process Data"
2. Выбрать "Rename"
3. Ввести новое имя в диалоге
4. ОК
```

## 🔧 Технические детали

### Логика показа меню:
```javascript
// Меню показывается для всех элементов всех типов диаграмм
const handleNodeContextMenu = useCallback((event, node) => {
  event.preventDefault()
  if (isLocked) return // Не показывать если заблокировано
  
  setContextMenu({
    x: event.clientX,
    y: event.clientY,
    node,
  })
}, [isLocked])
```

### Условное отображение кнопок:
```javascript
// "Edit Attributes" показывается только для ERD Entity/Relationship
{diagramType === 'erd' && 
 (isErdEntity(node) || isErdRelationship(node)) && (
  <button>Edit Attributes</button>
)}

// "Rename" и "Delete" показываются для всех типов диаграмм
<button>Rename</button>
<button>Delete</button>
```

### Каскадное удаление:
При удалении элемента:
1. Удаляется сам узел из массива nodes
2. Удаляются все связи (edges), где этот узел является source или target
3. Показывается уведомление с именем удаленного элемента

```javascript
setNodes((nds) => nds.filter((n) => n.id !== contextMenu.node.id))
setEdges((eds) =>
  eds.filter(
    (e) => e.source !== contextMenu.node.id && 
           e.target !== contextMenu.node.id
  )
)
```

## 📊 Сравнение функционала

| Действие | BPMN | ERD | DFD |
|----------|------|-----|-----|
| Edit Attributes | ❌ | ✅ | ❌ |
| Rename | ✅ | ✅ | ✅ |
| Delete | ✅ | ✅ | ✅ |

## 🎨 Стили меню

Контекстное меню использует:
- 🎨 Белый фон с тенью
- 📐 Скругленные углы (rounded-lg)
- 🖱️ Hover эффекты на кнопках
- 🎯 Z-index: 50 для отображения поверх всего
- ⚠️ Красный цвет для кнопки Delete

## 🚀 Преимущества

✅ Единообразный UX для всех типов диаграмм  
✅ Быстрый доступ к часто используемым операциям  
✅ Контекстно-зависимый функционал (ERD имеет доп. опции)  
✅ Интуитивно понятный интерфейс  
✅ Предотвращает случайное удаление (требуется 2 клика)  

## 🔮 Будущие улучшения

Возможные дополнения в будущем:
- 📋 Copy/Paste элементов
- 🎨 Изменение цвета элемента
- 🔄 Duplicate элемента
- 📏 Изменение размера
- 🔗 Быстрое создание связи
- 📝 Добавление комментариев

## ✨ Готово!

Контекстное меню работает для всех типов диаграмм с учетом их специфики!

